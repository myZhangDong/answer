# 010 `ui-next` 后台内容列表搜索 Tasks

## 使用方式

这份清单只跟踪 `010` 规格下 `ui-next` 后台文章、视频、项目列表搜索能力补齐，不与其他规格混写。

状态建议：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 已确认默认前提

- [x] 后台三类内容列表不复用前台综合搜索页的本地过滤实现
- [x] 后台三类内容列表不复用旧社区 `/answer/api/v1/search`
- [x] 三类页面统一使用 `query` 作为前端 URL 参数名
- [x] 视频、项目优先复用现有分页接口的 `search` 参数
- [x] 文章搜索在 `/answer/api/v1/content/page` 上补最小后端能力
- [x] 文章列表允许 `tag + query` 同时生效，且为 `AND` 关系

## Phase A：规格与主文档收口

目标：先把搜索边界、默认值和复用策略写清楚，避免后续误接前台综合搜索或旧社区搜索接口。

### 文档

- [x] 创建 `010` 独立规格文件
- [x] 创建 `010` 独立任务清单
- [x] 在 `specs/README.md` 中登记 `010`
- [x] 在主 `decisions.md` 中补充后台内容列表搜索决策
- [x] 在主 `tasks.md` 中同步更新独立子规格登记

验收：

- [x] `010` 的范围、默认值和边界已经固定
- [x] 后续实现默认按“列表分页接口搜索”推进，而不是重新设计后台综合搜索

## Phase B：后台 API 与数据层补口

目标：先让三类后台列表 API 都能接受统一的前端搜索条件。

### 前端 API 层

- [x] 为 `getAdminArticles` 增加关键字参数
- [x] 为 `getAdminVideos` 增加关键字参数
- [x] 为 `getAdminProjects` 增加关键字参数
- [x] 统一把前端 `query` 映射为后端请求参数 `search`

### 后端文章列表

- [x] 为 `ContentPageReq` 增加 `search` 入参
- [x] 在文章内容分页查询链路中透传 `search`
- [x] 为文章列表补标题模糊匹配
- [x] 保持 `content_type=2`、标签筛选和分页逻辑兼容

验收：

- [x] 三类后台列表请求都能接受关键字条件
- [x] 视频、项目继续复用现有后端搜索能力
- [x] 文章列表具备最小后端关键字搜索能力

## Phase C：文章列表搜索接入

目标：补齐文章列表的搜索输入、URL 状态和与标签筛选的联动。

### 页面

- [x] 新增文章列表搜索输入框
- [x] 支持回车和按钮触发搜索
- [x] 搜索后重置到第一页
- [x] 清空搜索时移除 `query`
- [x] 保持标签筛选与搜索条件可同时存在
- [x] 删除文章后刷新时保留当前标签和搜索状态

验收：

- [x] 文章列表支持 `tag + query` 联合过滤
- [x] 文章编辑返回列表时，状态可保留

## Phase D：视频与项目列表搜索接入

目标：在不改后端语义的前提下，让视频和项目列表拥有统一搜索交互。

### 视频列表

- [x] 新增视频列表搜索输入框
- [x] 支持回车和按钮触发搜索
- [x] 搜索后重置到第一页
- [x] 删除视频后刷新时保留搜索状态

### 项目列表

- [x] 新增项目列表搜索输入框
- [x] 支持回车和按钮触发搜索
- [x] 搜索后重置到第一页
- [x] 删除项目后刷新时保留搜索状态

验收：

- [x] 视频、项目列表都能按关键字搜索
- [x] 三类列表的 URL 状态和交互方式保持一致

## Phase E：回归验证

目标：确认搜索接入没有破坏原有分页、返回路径和构建结果。

### 检查项

- [x] 验证 `ui-next` 构建通过
- [x] 验证文章标签筛选未回归
- [x] 验证三类列表搜索后翻页仍正常
- [x] 验证从列表进入编辑页后返回，搜索状态保留
- [x] 验证删除后列表刷新仍保留当前搜索上下文
- [ ] 验证旧 `ui/` 未被误改

验收：

- [x] `010` 第一阶段达到“三类后台列表都可搜索 + 状态统一 + 文章补最小后端能力”的状态
