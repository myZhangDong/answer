# 008 `ui-next` 注册环信 CTA 与 Console 跳转埋点 Tasks

## 使用方式

这份清单只跟踪 `008` 规格下前台注册 CTA 收口、console 跳转参数统一和点击埋点接入，不与其他规格混写。

状态建议：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 已确认默认前提

- [x] 首页 Header 注册入口统一跳转到 `https://console.easemob.com/user/register?from=community`
- [x] 文章、视频、开源项目详情页底部 CTA 统一改为平台注册导流
- [x] 第一阶段不改旧 `ui/`
- [x] 第一阶段不新增后端埋点接口
- [x] 第一阶段埋点以 `ui-next` 前端公共工具收口

## Phase A：规格与主文档收口

目标：先把新口径写清楚，避免后续继续沿用旧的“后台发布”CTA 判断。

### 文档

- [x] 创建 `008` 独立规格文件
- [x] 创建 `008` 独立任务清单
- [x] 在 `specs/README.md` 中登记 `008`
- [x] 在主 `decisions.md` 中补充 CTA 切换决策
- [x] 在主 `tasks.md` 中同步更新独立子规格和当前进展

验收：

- [x] `008` 的范围、默认值和边界已经固定
- [x] 主规格文档不再继续把详情页 CTA 视为后台发布导向

## Phase B：公共注册跳转与埋点工具

目标：把链接、事件名和上报逻辑收口到一个公共出口。

### 工具层

- [x] 新增 `ui-next` 公共 console 注册跳转常量
- [x] 新增公共注册点击埋点函数
- [x] 埋点函数可区分不同 CTA 来源
- [x] 埋点函数支持补充内容上下文
- [x] 埋点逻辑不阻塞新窗口打开

验收：

- [x] 页面组件不再手写注册链接和事件名
- [x] 后续新增同类 CTA 时可直接复用公共工具

## Phase C：首页 Header CTA 收口

目标：统一首页 Header 的注册跳转地址和埋点。

### Header

- [x] 修改桌面端 Header 注册按钮
- [x] 修改移动端抽屉内注册按钮
- [x] 两处注册按钮统一带 `from=community`
- [x] 两处注册按钮点击时补位置埋点

验收：

- [x] Header 两处注册入口都跳到同一个 console 地址
- [x] Header 两处注册入口都可区分点击来源

## Phase D：详情页底部 CTA 收口

目标：把三类详情页底部 CTA 统一成平台注册导流。

### 公共 CTA 组件

- [x] 修改 `ui-next/src/app/components/CtaBanner.tsx`
- [x] 移除“登录后台发布 / 进入后台管理”逻辑
- [x] 更新标题、说明和按钮文案
- [x] 点击按钮时补公共埋点

### 页面接入

- [x] 文章详情页接入新 CTA 来源信息
- [x] 视频详情页接入新 CTA 来源信息
- [x] 开源项目详情页接入新 CTA 来源信息
- [x] 三类详情页 CTA 都跳到同一个 console 地址

验收：

- [x] 三类详情页底部 CTA 已统一为注册环信平台导向
- [x] 三类详情页 CTA 点击都能区分来源位置

## Phase E：回归验证

目标：确认跳转参数、文案和构建结果都没有回归。

### 检查项

- [x] 验证 `ui-next` 构建通过
- [x] 验证 Header 注册入口仍正常显示
- [x] 验证详情页底部 CTA 文案更新生效
- [x] 验证注册链接都带 `from=community`
- [x] 验证旧 `ui/` 未被误改

验收：

- [x] `008` 第一阶段达到“注册 CTA 统一 + 带来源参数 + 带位置埋点”的状态

