# 007 `ui-next` 首页右侧热门内容 Tasks

## 使用方式

这份清单只跟踪 `007` 规格下首页右侧“热门 Demo”“热门教程”的真实数据接入、项目 `hot` 排序修正，以及详情页同类模块收口，不与内容门户主任务清单混写。

状态建议：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 已确认默认前提

- [x] 热门 Demo 使用开源项目数据
- [x] 热门教程使用视频教程数据
- [x] 第一阶段热门排序只看浏览量，不引入点赞、评分和手工置顶
- [x] 热门 Demo 默认展示 2 条
- [x] 热门教程默认展示 4 条
- [x] 热门 Demo 浏览量第一阶段继续复用关联文章 `question.view_count`
- [x] 第一阶段不新增首页专用聚合接口，优先复用现有分页接口
- [x] 热门模块无数据时默认展示轻量空态
- [x] 首页实现时，同步把视频详情页和项目详情页右栏同类静态模块一起改为真实数据

## Phase A：后端 `hot` 排序语义修正

目标：先把项目公开列表接口的 `order=hot` 语义修正为“按浏览量排序”，避免首页接线后仍展示错误结果。

### 项目列表排序

- [x] 确认 `GET /answer/api/v1/project/page?order=hot` 当前未按浏览量排序
- [x] 修正 `internal/repo/project/project_repo.go` 中 `hot` 分支
- [x] 让项目 `hot` 排序按关联文章 `question.view_count DESC`
- [x] 浏览量相同情况下补充创建时间倒序稳定排序
- [ ] 确认 `newest`、`active` 等其他排序语义不被回归破坏

### 接口语义校验

- [x] 确认项目公开列表响应继续返回 `view_count`
- [x] 确认首页与详情页后续可直接复用 `project/page?order=hot`
- [x] 如实现过程中需要最小 service/repo 调整，保持 controller/router 不额外扩张

验收：

- [~] 项目公开列表 `order=hot` 已真正按浏览量返回
- [x] 后端无需新增首页专用聚合接口即可支撑 007 前端接线

## Phase B：`ui-next` 热门内容前端 API 与组件收口

目标：先整理请求层和侧栏组件，消除组件内部硬编码假数据来源。

### API 封装

- [x] 在 `ui-next/src/app/api/contentApi.ts` 中补热门 Demo 读取封装
- [x] 在 `ui-next/src/app/api/contentApi.ts` 中补热门教程读取封装
- [x] 热门 Demo 请求固定使用 `page=1&page_size=2&order=hot`
- [x] 热门教程请求固定使用 `page=1&page_size=4&order=hot`
- [x] 保持接口失败不阻塞页面主体内容

### 组件收口

- [x] 改造 `ui-next/src/app/components/SidebarWidgets.tsx`
- [x] 移除组件内部 `HOT_TUTORIALS` 静态数组
- [x] 移除组件内部或调用侧对 `HOT_DEMOS` 静态数组的正式依赖
- [x] 让热门 Demo 组件接收真实列表数据
- [x] 让热门教程组件接收真实列表数据
- [x] 让两类组件支持真实详情跳转链接
- [x] 为两类组件补轻量空态展示

验收：

- [x] 侧栏组件已成为纯展示组件，不再以内置假数据驱动
- [x] 请求层已具备热门 Demo、热门教程的统一读取入口

## Phase C：首页右栏真实数据接入

目标：让首页右栏“热门 Demo”“热门教程”替换为真实内容，同时不影响“周热门文章”和现有布局。

### 首页数据并行加载

- [x] 修改 `ui-next/src/app/pages/Home.tsx`
- [x] 首页并行读取周热门文章、热门 Demo、热门教程
- [x] 热门 Demo 请求失败时不影响首页文章主体渲染
- [x] 热门教程请求失败时不影响首页文章主体渲染

### 首页模块替换

- [x] 移除首页 `HOT_DEMOS` 静态数组
- [x] 首页“热门 Demo”改为展示真实项目
- [x] 首页“热门教程”改为展示真实视频
- [x] 热门 Demo 条目点击进入 `/project/:id`
- [x] 热门教程条目点击进入 `/video/:id`
- [x] 返回数量不足时按实际条数展示
- [x] 无数据时展示轻量空态，不回退假数据

### 响应式保持

- [x] 桌面端继续保持首页右栏布局
- [x] 移动端继续保持热门模块下沉顺序
- [~] 不因新增加载和空态破坏 `006` 已完成的首页移动端适配

验收：

- [x] 首页右栏两块热门模块都已展示真实数据
- [x] 首页不再依赖静态热门 Demo / 热门教程占位数组
- [~] 首页移动端和桌面端布局都未回归

## Phase D：视频详情页与项目详情页同类模块同步收口

目标：避免首页已切真实数据，但详情页右栏仍停留在静态假数据，造成产品口径分裂。

### 视频详情页

- [x] 修改 `ui-next/src/app/pages/VideoDetail.tsx`
- [x] 详情页“热门教程”改为展示真实视频教程数据
- [x] 条目点击进入对应 `/video/:id`
- [x] 无数据时展示轻量空态

### 项目详情页

- [x] 修改 `ui-next/src/app/pages/OpenSourceDetail.tsx`
- [x] 详情页“热门 Demo”改为展示真实开源项目数据
- [x] 条目点击进入对应 `/project/:id`
- [x] 无数据时展示轻量空态

### 共享口径

- [x] 首页与详情页复用同一热门数据口径
- [x] 首页与详情页复用同一套侧栏展示组件
- [x] 详情页不新增独立推荐算法或额外后端接口

验收：

- [x] 视频详情页右栏“热门教程”已切到真实数据
- [x] 项目详情页右栏“热门 Demo”已切到真实数据
- [x] 首页与详情页相关模块的数据口径一致

## Phase E：回归、联调与文档收口

目标：确认 007 第一阶段达到“首页和详情页热门模块都走真实数据，项目 `hot` 语义正确”的可交付状态。

### 联调检查

- [~] 验证项目公开列表 `order=hot` 返回顺序正确
- [~] 验证视频公开列表 `order=hot` 返回顺序正确
- [~] 验证首页热门 Demo 展示条目数和顺序正确
- [~] 验证首页热门教程展示条目数和顺序正确
- [~] 验证视频详情页热门教程展示正确
- [~] 验证项目详情页热门 Demo 展示正确
- [~] 验证条目点击跳转到正确详情页

### 空态与失败态

- [ ] 验证热门 Demo 无数据时显示轻量空态
- [ ] 验证热门教程无数据时显示轻量空态
- [ ] 验证接口失败时首页主体内容仍可正常使用
- [ ] 验证接口失败时详情页主体内容仍可正常使用

### 回归检查

- [~] 验证首页“周热门文章”不受本轮改动影响
- [~] 验证首页广告位、Banner 和文章列表不受本轮改动影响
- [~] 验证视频详情页正文、播放器、互动组件未被改坏
- [~] 验证项目详情页正文、CTA、互动组件未被改坏
- [ ] 验证旧 `ui/` 未被误改

### 文档收口

- [x] 已创建 `007` 独立任务清单
- [ ] 如实现过程中形成新的边界决策，补充到 `decisions.md`
- [x] 根据实现进度持续更新本文件状态

验收：

- [~] `007` 第一阶段达到“首页和详情页热门模块真实化 + 项目 hot 语义修正”的状态
- [x] 规格、任务与实现边界保持一致
