# 005 `ui-next` 内容标签与视频分类管理 Tasks

## 使用方式

这份清单只跟踪 `005` 规格下的文章标签管理、文章标签录入闭环和视频分类配置化，不与内容门户主任务清单混写。

状态建议：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 已确认默认前提

- [x] 文章继续使用后端真实标签，不新增文章独立标签模型
- [x] 视频继续使用现有 `category/type` 字段，不并入文章标签表
- [x] 视频分类第一阶段采用 `ui-next` 前端集中配置，不先做后端可编辑配置
- [x] 视频默认分类第一版继续沿用：基础篇、进阶篇、群组篇、AI篇
- [x] 文章标签管理第一阶段至少支持：列表、创建、编辑、删除
- [x] 如复用后端现有能力，恢复标签和标签描述维护可同轮补齐
- [x] 第一阶段不覆盖项目标签管理和跨内容统一 taxonomy 模型

## Phase A：文章标签后端能力梳理与前端 API 封装

目标：确认并封装 `ui-next` 所需的标签管理接口，避免文章录入继续依赖“后端已有数据但后台无法维护”的断层状态。

### 接口梳理

- [x] 确认继续复用 `GET /answer/api/v1/question/tags`
- [x] 确认继续复用 `POST /answer/api/v1/tag`
- [x] 确认继续复用 `PUT /answer/api/v1/tag`
- [x] 确认继续复用 `DELETE /answer/api/v1/tag`
- [x] 确认同轮复用 `POST /answer/api/v1/tag/recover`
- [x] 确认标签描述字段写入和回显所需的请求结构

### `ui-next` API 封装

- [x] 新增文章标签管理 API 封装
- [x] 封装标签列表读取
- [x] 封装标签创建
- [x] 封装标签编辑
- [x] 封装标签删除
- [x] 封装标签恢复
- [x] 封装标签描述字段读写
- [x] 统一标签接口错误提示与权限失败提示

验收：

- [x] `ui-next` 已具备完整的文章标签管理 API 封装
- [x] 前端不再需要绕过后台页面、手工依赖数据库已有标签才能创建文章

## Phase B：`ui-next` 文章标签管理页

目标：新增独立后台页面，让管理员可直接维护文章标签。

### 路由与导航

- [x] 新增 `/admin/article-tags`
- [x] 后台导航新增“文章标签”
- [x] 管理后台默认入口仍保持 `/admin/articles`
- [x] 当前激活态与返回路径高亮正确

### 页面结构

- [x] 新增 `AdminArticleTags` 页面
- [x] 页面展示标签列表
- [x] 页面展示标签基础信息：至少包含 `display_name`、`slug_name`
- [x] 如接口返回足够字段，补充展示标签描述、状态或推荐信息
- [x] 提供统一成功和失败提示

### 标签操作

- [x] 支持创建标签
- [x] 支持编辑标签
- [x] 支持删除标签
- [x] 支持恢复标签
- [x] 支持编辑标签描述

### 权限与容错

- [x] 无权限或接口失败时提供明确提示
- [x] 标签创建重复、名称冲突时提供明确提示
- [x] 删除失败时不影响列表继续使用

验收：

- [x] 管理员可在 `ui-next` 内创建文章标签
- [x] 管理员可在 `ui-next` 内编辑和删除文章标签
- [x] 管理员可恢复上一个已删除标签

## Phase C：文章录入与筛选链路联动

目标：让文章列表筛选、创建页、编辑页共同依赖同一套标签数据源，并补齐从“没标签”到“可发布文章”的后台闭环。

### 列表与编辑页统一

- [x] 文章管理列表继续使用同一套标签源
- [x] 新建文章页继续使用同一套标签源
- [x] 编辑文章页继续使用同一套标签源
- [x] 标签为空时提供明确空状态提示，不再只显示空下拉

### 路由联动

- [x] 新建文章页提供“去创建标签”入口
- [x] 编辑文章页提供“去创建标签”入口
- [x] 从标签管理页返回后可继续当前文章编辑流程
- [x] 返回文章页后可重新拉取标签选项

### 交互收口

- [x] 标签创建成功后文章创建页可立即选择新标签
- [x] 标签删除后文章列表筛选和文章编辑页正确处理失效选项
- [x] 已有关联标签的文章编辑页在标签变更后回显行为明确

验收：

- [x] 管理员可以从“无标签”状态完成“创建标签 -> 返回文章页 -> 选择标签 -> 提交文章”的前端闭环
- [x] 文章列表筛选、创建、编辑三个入口的标签口径一致

## Phase D：视频分类配置化

目标：去掉视频分类在页面组件中的局部硬编码，建立 `ui-next` 内部统一配置源。

### 配置源收口

- [x] 新增视频分类集中配置文件
- [x] 配置文件收录第一版默认分类：基础篇、进阶篇、群组篇、AI篇
- [x] 为分类项约定统一结构，例如 `label`、`value`
- [x] 明确未知分类值的兜底展示策略

### 后台消费

- [x] `AdminVideoEditor` 改为从集中配置源读取分类选项
- [x] 视频编辑页默认值与已有数据兼容
- [x] 视频编辑页校验与后端写入字段继续保持 `category/type` 口径

### 前台消费

- [x] 视频列表页如有分类筛选或展示，改为消费统一配置源
- [ ] 视频详情页如有分类展示，改为消费统一配置源
- [x] 前后台分类文案保持一致

验收：

- [x] `ui-next` 内不再存在分散的 `VIDEO_CATEGORIES` 局部硬编码
- [~] 视频后台编辑、前台展示和筛选入口共用同一份分类配置
- [ ] 历史视频数据的分类展示未被改坏

## Phase E：联调、回归与文档收口

目标：确认 `005` 第一阶段达到“文章标签可管理、视频分类已配置化”的可交付状态。

### 联调检查

- [ ] 验证文章标签列表可读取
- [ ] 验证文章标签创建可用
- [ ] 验证文章标签编辑可用
- [ ] 验证文章标签删除可用
- [ ] 如已接入恢复能力，验证标签恢复可用
- [ ] 验证文章创建页可立即选择新建标签
- [ ] 验证文章编辑页标签回显正确
- [ ] 验证文章列表标签筛选正确
- [ ] 验证视频编辑页分类选项来自统一配置

### 回归检查

- [ ] 验证文章发布和编辑链路未被标签管理页改坏
- [ ] 验证视频创建和编辑链路未被分类配置化改坏
- [ ] 验证前台文章标签展示口径未回归
- [ ] 验证前台视频分类展示口径未回归
- [ ] 验证旧 `ui/` 未被误改

### 文档收口

- [x] 如实现中新增细节结论，补充到 `decisions.md`
- [x] 已根据本轮实现更新本文件状态

验收：

- [~] `005` 第一阶段已进入“文章标签管理闭环 + 视频分类配置化”联调阶段
- [x] 规格、任务和当前实现边界一致

## 当前未决问题

- [ ] 暂无新的阻塞性未决问题；若后续要把视频分类升级为后台可编辑配置，应在 `005` 下另开增量决策或后续任务
