# `ui-next` 管理员登录与现有鉴权复用 Tasks

## 使用方式

这份清单只跟踪 `ui-next` 管理员登录、会话恢复、后台路由守卫和现有后端鉴权复用，不再与内容门户主任务清单混写。

状态建议：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 已确认默认前提

- [x] 本轮只支持邮箱 + 密码的管理员登录
- [x] `captcha`、User Center、Connector 登录适配先作为兼容项，不阻塞主链路
- [x] 本轮只做认证骨架、会话恢复、退出登录、后台路由守卫、请求层会话复用
- [x] `ui-next` 新增显式 `/admin/login` 路由
- [x] 非管理员访问 `ui-next /admin` 时返回 `403` 页面
- [x] 旧 `ui/` 后台继续保留，作为迁移期间 fallback

## Phase A：认证骨架接通

目标：让 `ui-next` 具备真实管理员登录和后台路由准入能力。

### 路由与页面

- [x] 新增 `ui-next` 管理员登录页
- [x] 将 `/admin` 改为受保护路由
- [x] 新增 `/admin/login`
- [x] 非管理员访问 `/admin` 时返回 `403` 或跳到约定错误页
- [x] 删除 `ui-next/src/app/pages/Admin.tsx` 内部的演示密码门逻辑

### 鉴权 API 封装

- [x] 新增 `ui-next` 管理员认证 API 封装
- [x] 对接 `POST /answer/api/v1/user/login/email`
- [x] 对接 `GET /answer/api/v1/user/info`
- [x] 对接 `GET /answer/api/v1/user/logout`
- [x] 明确登录请求入参沿用 `e_mail`、`pass`
- [x] 预留 `captcha_id`、`captcha_code` 字段，但第一轮不强制接 UI

### 登录态管理

- [x] 新增 `ui-next` 管理员身份状态容器
- [x] 启动或进入后台路由前拉取当前用户信息
- [x] 以 `role_id === 2` 作为管理员准入判定
- [x] 会话失效时清空本地管理员状态

验收：

- [x] 管理员可以通过真实邮箱密码登录 `ui-next`
- [x] 未登录访问 `/admin` 时不会直接进入后台内容
- [x] 非管理员访问 `/admin` 时不会进入后台页面

## Phase B：会话恢复与退出登录

目标：让 `ui-next` 后台登录态可持续使用，而不是一次性页面状态。

### 会话恢复

- [x] 页面刷新后通过 `/answer/api/v1/user/info` 恢复管理员身份
- [x] 已登录管理员直接访问 `/admin/login` 时跳回 `/admin`
- [x] 登录态恢复失败时回到 `/admin/login`

### 退出登录

- [x] 对接 `GET /answer/api/v1/user/logout`
- [x] 退出后清理 `ui-next` 管理员状态
- [x] 退出后跳回 `/admin/login`

### 错误处理

- [x] 统一登录失败提示
- [x] 统一会话过期处理
- [x] 统一 `403` 状态表现

验收：

- [x] 管理员刷新页面后仍能留在后台
- [x] 管理员退出后不能继续访问受保护后台路由
- [x] 会话过期后不会停留在错误后台状态

## Phase C：后台请求层复用现有会话

目标：修正 `ui-next` 后台请求层的认证假设，统一复用现有 Cookie / Session。

### 请求层

- [x] 统一后台请求复用 `ui-next/src/app/api/client.ts`
- [x] 确认后台请求继续使用 `credentials: "include"`
- [x] 移除 `ui-next/src/app/api/adminApi.ts` 中 Bearer Token 主认证假设
- [x] 统一后台请求错误处理

### `adminApi` 修正

- [x] 将 `adminApi.ts` 的认证说明改为复用现有后端会话
- [x] 清理与当前后端不匹配的占位说明
- [x] 保留后续内容管理接口接线所需的最小封装结构

验收：

- [x] `ui-next` 后台请求不再依赖前端自管 Token
- [x] 已登录管理员发起后台请求时能自动复用现有会话

## Phase D：最小联调与迁移准备

目标：确认 `ui-next` 后台认证链路已具备继续承接管理页面的前提。

### 联调检查

- [x] 验证登录成功跳转
- [x] 验证刷新恢复登录态
- [x] 验证退出登录
- [x] 验证非管理员拦截
- [x] 验证未登录直接访问后台时的跳转

### 兼容记录

- [ ] 记录当前环境是否启用登录验证码
- [ ] 记录当前环境是否启用 User Center / Connector 接管登录
- [ ] 如存在插件登录差异，补入独立决策或开放问题

### 迁移衔接

- [x] 明确下一阶段先接哪一个后台页面闭环
- [x] 明确旧 `ui/` 后台在迁移完成前继续保留

验收：

- [x] `ui-next` 管理员认证链路达到“可继续迁移后台页面”的状态
- [ ] 已知插件和验证码风险被记录，而不是隐含在聊天记录里

备注：

- 已完成 `ui-next` 管理员认证骨架、会话恢复、退出登录、后台守卫和请求层会话复用
- `ui-next` 本地执行 `npm run build` 已通过（2026-03-26）
- 已完成一轮人工联调，确认登录、刷新恢复、退出和后台守卫链路正常
- 下一阶段优先接入文章后台闭环，继续复用 `question(type=2)` 现有后端能力
- Phase D 剩余风险主要集中在验证码、插件接管登录和 User Center 兼容项记录
