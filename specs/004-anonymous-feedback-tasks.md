# 004 匿名点赞与匿名评分 Tasks

## 使用方式

这份清单只跟踪 `004` 规格下的匿名点赞与匿名评分实现，不与内容门户主任务清单混写。

状态建议：

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成

## 已确认默认前提

- [x] 第一阶段同时覆盖文章、视频、项目三类内容
- [x] 匿名点赞不支持取消，重复提交按幂等处理
- [x] 第一阶段列表页不展示评分，只展示点赞数
- [x] 匿名 `token` 默认有效期先设置为 3 个月
- [x] 新匿名互动统计不兼容历史 `vote_count`，从 0 开始累计
- [x] 首页热门内容排序第一阶段不引入评分维度，继续只和浏览次数有关
- [x] 同一匿名 `token` 对同一内容只能点赞一次、评分一次
- [x] 同一游客对同一内容对象允许 1 次点赞和 1 次评分，并持久化到 `localStorage`
- [x] 详情页交互按“点赞后可评分，评分后整体禁用”收口

## Phase A：后端匿名互动基础能力

目标：补齐匿名互动的数据模型、接口和聚合逻辑，让三类内容都能读取和提交真实互动数据。

### 数据结构与迁移

- [x] 新增匿名互动聚合表
- [x] 新增匿名互动记录表
- [x] 为聚合表补齐索引和唯一约束
- [x] 为记录表补齐索引和唯一约束
- [x] 新增数据库 migration

### Schema 与 Repo

- [x] 新增互动聚合读取请求与响应 schema
- [x] 新增点赞提交请求与响应 schema
- [x] 新增评分提交请求与响应 schema
- [x] 新增匿名互动 repo
- [x] 补齐点赞幂等逻辑
- [x] 补齐评分覆盖逻辑

### Service 与 Controller

- [x] 新增匿名互动 service
- [x] 新增匿名互动 controller
- [x] 统一校验 `object_type` 只允许 `article`、`video`、`project`
- [x] 校验目标对象是否存在且可公开访问
- [x] 实现匿名 `token` 下发与读取
- [x] 实现匿名 `token` 默认 3 个月有效期
- [x] 读取并返回当前匿名访客互动状态
- [x] 拒绝同一匿名 `token` 的重复点赞
- [x] 拒绝同一匿名 `token` 的重复评分
- [ ] 实现基础限流与异常日志

### Router 与装配

- [x] 新增 `GET /answer/api/v1/content/feedback`
- [x] 新增 `POST /answer/api/v1/content/feedback/like`
- [x] 新增 `POST /answer/api/v1/content/feedback/rating`
- [x] 新增 `POST /answer/api/v1/content/feedback/batch`
- [x] 将新接口接入 router
- [x] 将新 service / repo 接入 provider
- [x] 如修改 Wire，同步更新 `cmd/wire_gen.go`

验收：

- [x] 三类内容都能读取聚合互动数据
- [x] 三类内容都能提交匿名点赞
- [x] 三类内容都能提交匿名评分
- [x] 重复点赞不会重复累加
- [x] 重复评分只覆盖，不重复增加评分人数

## Phase B：`ui-next` 详情页接入真实互动

目标：让 `ui-next` 文章、视频、项目详情页都接入真实匿名互动接口，替换当前本地假交互。

### API 封装

- [x] 新增 `ui-next` 匿名互动 API 封装
- [x] 封装详情页读取互动聚合接口
- [x] 封装点赞提交接口
- [x] 封装评分提交接口
- [x] 封装当前匿名访客互动状态字段

### 文章详情

- [x] `ArticleDetail` 改读真实互动聚合数据
- [x] 替换当前本地点赞逻辑
- [x] 替换当前本地评分逻辑
- [x] 点赞成功后刷新点赞数
- [x] 评分成功后刷新平均分和评分人数
- [x] 已点赞后禁用文章详情点赞按钮
- [x] 已评分后禁用文章详情评分交互

### 视频详情

- [x] `VideoDetail` 改读真实互动聚合数据
- [x] 接入匿名点赞
- [x] 接入匿名评分
- [x] 保持原有视频正文和播放器逻辑不受影响
- [x] 已点赞后禁用视频详情点赞按钮
- [x] 已评分后禁用视频详情评分交互

### 项目详情

- [x] `OpenSourceDetail` 或项目详情页改读真实互动聚合数据
- [x] 接入匿名点赞
- [x] 接入匿名评分
- [x] 保持项目仓库链接和正文展示逻辑不受影响
- [x] 已点赞后禁用项目详情点赞按钮
- [x] 已评分后禁用项目详情评分交互

### 失败与反馈

- [x] 点赞失败时提供统一轻提示
- [x] 评分失败时提供统一轻提示
- [x] 互动接口失败时不影响详情页主体内容渲染
- [x] 将游客互动状态持久化到 `localStorage`
- [x] 页面刷新或重新进入后恢复游客互动禁用态
- [x] 点赞与评分分别按游客本地状态单独禁用

验收：

- [x] 三类详情页都能看到真实点赞数
- [x] 三类详情页都能看到真实平均分和评分人数
- [x] 无评分时统一显示“暂无评分”
- [x] 当前本地假点赞和假评分状态已移除

## Phase C：列表页与首页展示收口

目标：让列表页统一展示真实点赞数，并保持“列表不展示评分、热门排序不引入评分”的已确认口径。

### 列表数据接入

- [x] 文章列表接入真实点赞数
- [x] 视频列表接入真实点赞数
- [x] 项目列表接入真实点赞数
- [x] 如列表读取性能不足，补充批量聚合查询接口

### 展示口径收口

- [x] 确认列表页不展示 `rating_avg`
- [x] 确认列表页不展示 `rating_count`
- [x] 确认首页热门内容排序不引入评分维度
- [x] 确认首页热门内容排序继续只和浏览次数有关

### 旧字段依赖清理

- [x] 清理 `ui-next` 中对本地假点赞数的依赖
- [x] 清理 `ui-next` 中对旧 `vote_count` 展示口径的依赖
- [x] 确认不需要兼容历史 `question.vote_count`

验收：

- [x] 三类列表页都展示真实点赞数
- [x] 三类列表页都不展示评分
- [x] 首页热门内容排序口径未被匿名评分改动

## Phase D：测试、联调与回归

目标：确认匿名互动在三类内容上稳定可用，并且不影响现有内容门户主流程。

### 后端验证

- [ ] 验证匿名 `token` 首次提交时会正确下发
- [ ] 验证匿名 `token` 3 个月有效期配置正确
- [ ] 验证重复点赞请求会被拒绝
- [ ] 验证重复评分请求会被拒绝
- [ ] 验证非法 `object_type` 会被拒绝
- [ ] 验证不存在的内容对象会被拒绝

### 前端联调

- [ ] 验证文章详情点赞与评分可用
- [ ] 验证视频详情点赞与评分可用
- [ ] 验证项目详情点赞与评分可用
- [ ] 验证文章、视频、项目列表点赞数展示正确
- [ ] 验证列表页没有评分展示

### 回归检查

- [ ] 验证浏览量逻辑未被改坏
- [ ] 验证首页热门内容排序仍按浏览次数工作
- [ ] 验证文章、视频、项目详情主体内容不受互动接口失败影响
- [ ] 验证旧社区 `vote` 接口未被误删

验收：

- [ ] 三类内容匿名互动闭环可用
- [ ] 列表与详情展示口径符合 `004` 规格
- [ ] 首页热门排序口径未回归

## 当前未决问题

- [ ] 暂无新的阻塞性未决问题；如后续要把评分引入热门排序，需单独立项或补决策
